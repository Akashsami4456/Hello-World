version: 2.1

jobs:
  say-hello:
    docker:
      - image: cimg/base:current

    steps:
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
      - checkout
      - store_artifacts:
          path: ./test_data/go
          destination: report.xml
  

workflows:
  say-hello-workflow: 
    jobs:
      - say-hello
